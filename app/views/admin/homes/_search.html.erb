<%= form_with url: admin_search_path, method: :get, local: true do |f| %>
  <%= f.text_field :content %>
  <%= f.select :model, options_for_select({ "会員"=> "member", "投稿記事" => "post" }) %>
  <div class="text-right"><%= submit_tag '検索', class: "btn btn-primary mt-2" %></div>
<% end %>